{
  "name": "mumega-motion-theme",
  "version": "0.1.0",
  "private": true,
  "description": "Mumega WordPress theme with React/Motion-powered interactive components. Shares WordPress core's own React instance instead of bundling a duplicate.",
  "scripts": {
    "build": "wp-scripts build src/js/index.js --output-path=build",
    "start": "wp-scripts start src/js/index.js --output-path=build",
    "test": "npm run test:js && npm run build",
    "test:js": "wp-scripts test-unit-js --runInBand",
    "package": "npm run build && ./scripts/package-theme.sh"
  },
  "dependencies": {
    "motion": "^12.42.2"
  },
  "devDependencies": {
    "@wordpress/scripts": "^30.0.0"
  }
}
